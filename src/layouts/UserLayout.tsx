import React from 'react';

const UserLayout: React.FC = ({ children }) => {
    return (
        <div>
            userLayout
            {children}
        </div>
    );
};

export default UserLayout;
